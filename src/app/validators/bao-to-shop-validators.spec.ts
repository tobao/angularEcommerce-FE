import { BaoToShopValidators } from './bao-to-shop-validators';

describe('BaoToShopValidator', () => {
  it('should create an instance', () => {
    expect(new BaoToShopValidators()).toBeTruthy();
  });
});
